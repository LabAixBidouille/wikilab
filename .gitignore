node_modules/
site/build/
site/.docusaurus/
site/.cache-loader/
.DS_Store
.env*
*.log

# Artefacts de compilation TypeScript (tsc) — le projet utilise .ts/.tsx en source
site/**/*.js

# Caches outillage racine
.cache/
.lychee/
.cspell-cache

# Vidéos (souvent >100MB, limite GitHub) — à gérer via Git LFS si besoin
*.mp4
*.mov
*.webm

# Claude Code working artifacts
.claude/
